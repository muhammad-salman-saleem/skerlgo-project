/*
 * leave empty on purpose
 * use sub folders to group models by purpose
 * eg: users, business, security, layout, utils
 *
 * sub folders should export all their content
 * sub folders should group and export types in a top ./types file
 * you may sub-group models if necessary
 * each sub-group must export it's content and sub-folders must export the sub-group
 */
